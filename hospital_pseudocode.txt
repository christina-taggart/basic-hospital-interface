-- Hospital interface --

Release 0:

I need a hospital object
	this will need a name
		a location
		a number of employees
		a number of patients

I need a patient object
	this will need a name
		a patient id
		an illness
		a billing address
		# not sure if the patient should have a doctor

I need an employee object
	this could include types as
		doctor
		receptionists
		janitors

Release 1:

- Patients and Employees should have user names and passwords
- I need a interface class that allows for input and output between a user and the terminal